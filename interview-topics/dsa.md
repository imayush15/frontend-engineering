# 1. Flatten Nested Object

Write a function flattenObject(obj) that converts a nested object into a flat object.

```js
const input = {
  user: {
    name: "Ayush",
    address: {
      city: "Bangalore",
      pin: 560001,
    },
  },
  active: true,
};

flattenObject(input);
```
